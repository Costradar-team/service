# CostRadar Project Status

마지막 갱신: 2026-08-29 (Asia/Seoul)

이 문서는 현재 프로젝트의 진행 상황을 기록한다. 에이전트는 `AGENTS.md`를 읽은
직후 이 문서를 끝까지 읽고, 실제 Git 상태 및 테스트 결과와 대조해야 한다.

## 한눈에 보는 진행률

| 범위 | 추정 진행률 | 판단 기준 |
|---|---:|---|
| 로컬 데이터 파이프라인 | 85% | 원본 CSV 프로파일링, 매핑, 정제와 리포트 생성 가능 |
| 로컬 ML 프로토타입 | 90% | 네 예측 단위, 지점 직접예측, 다중 시점 예측과 테스트 구현 |
| DB 연동 | 15% | ERD와 MySQL 실행 설정만 존재 |
| 백엔드 연동 | 30% | 네 예측 단위의 전달용 JSON 생성까지만 존재 |
| 자동화·배포 | 10% | 로컬 스크립트만 있고 스케줄러·배포·모니터링 없음 |
| 전체 서비스 | 약 50% | 지점 예측 JSON까지 동작하지만 DB·API·서비스 연결 미완성 |

진행률은 코드량이 아니라 사용자에게 제공 가능한 전체 흐름을 기준으로 한 추정치다.

## 현재 Git 상태

- 작업 브랜치: `feature/ml-pipeline`
- 기준 브랜치: `origin/main`
- 기준 커밋: `69730c4`
- main 위의 재배치된 ML 커밋: `1fec6d8`
- 로컬 브랜치는 `origin/main`에서 직접 분기된 상태다.
- 원격 `feature/ml-pipeline`은 재배치 전 커밋 `715cc07`을 가리키는 상태로 마지막
  확인됐다.
- 기존 브랜치 이름을 계속 사용한다. 사용자의 요청 없이 새 브랜치를 만들지 않는다.
- 원격 기록과 로컬 기록이 달라 일반 push는 non-fast-forward로 거절된다. 같은
  원격 브랜치를 갱신하려면 사용자 승인 후 `--force-with-lease`가 필요하다.
- 현재 최신 기능 변경과 문서 변경은 로컬 미커밋 상태이며 아직 GitHub에 반영되지
  않았다.

push 전에는 원격 상태를 다시 조회하고, 최신 `origin/main`을 반영해 충돌과 테스트를
확인한다. PR 병합 후에는 팀 규칙에 따라 원격 feature 브랜치를 삭제한다.

## 완료된 데이터 작업

- 한국소비자원 원본 CSV 13개를 입력으로 사용한다.
- 원본 3,062,819행을 프로파일링하고 상품 매핑을 적용한다.
- 밀가루, 설탕, 버터, 계란, 우유 대상 204,036행을 전처리한다.
- 상품명·판매업소·조사일 기준 중복과 충돌을 검사한다.
- 단위가격을 다음 기준으로 표준화한다.
  - 밀가루·설탕·버터: `KRW/kg`
  - 우유: `KRW/L`
  - 계란: `KRW/10ea`
- 전처리 결과는 `artifacts/processed/kca_prices_processed.csv`에 생성한다.
- 판매업소 접두어를 기준으로 10개 브랜드와 466개 지점을 구분한다.

## 완료된 ML 작업

- 세부유형별 모델 데이터: 463행, 19개 시계열
- 상품명별 모델 데이터: 895행, 39개 시계열
- 브랜드별 모델 데이터: 4,612행, 222개 시계열
- 지점별 실제가격 모델 데이터: 204,036행, 9,615개 시계열
- 지점 시계열 중 최소 6회 이상 실제가격이 있는 9,083개를 예측 대상으로 사용한다.
- 날짜순 백테스트와 다음 기준선을 구현했다.
  - `naive_last_value`
  - 최근 4회 `rolling_mean`
- Gradient Boosting 모델과 선택적 LightGBM 경로를 구현했다.
- 세부유형, 상품명, 브랜드, 지점 모델을 각각 저장한다.
- 지점 모델은 브랜드 평균에 보정계수를 적용하지 않는다. `상품명 × 브랜드 × 지점`
  실제 단위가격을 직접 목표값으로 사용하는 하나의 글로벌 모델이다.
- 학습과 예측을 분리했다.
  - `-TrainModel` 사용 시에만 학습
  - 평상시에는 저장된 `price_model.joblib`로 예측
- 학습 시 백테스트 후 전체 모델링 가능 이력으로 운영 모델을 다시 적합한다.
- `ForecastHorizon`을 사용한 다중 미래 조사 시점 재귀 예측을 구현했다.
  - 1단계는 최신 실제 가격을 사용한다.
  - 2단계부터는 직전 단계 ML 예측값을 입력 이력에 추가한다.
  - 각 단계에서 lag와 이동통계를 다시 계산한다.
- 네 예측 단위 모두 백엔드 전달 형식 JSON으로 변환한다.

## 현재 모델 성능

모델 학습 기준 마지막 실제 조사일은 2026-07-24다.

| 예측 단위 | ML sMAPE | 직전 가격 기준선 sMAPE |
|---|---:|---:|
| 세부유형 | 1.2258% | 0.8824% |
| 상품명 | 1.4338% | 1.0822% |
| 브랜드 | 0.8904% | 0.8062% |
| 지점 직접가격 | 1.5455% | 1.2062% |

현재 네 단위 모두 ML 모델이 직전 가격 기준보다 성능이 낮다. 직전 가격은 내부
백테스트 기준선으로만 유지하며 예측 CSV와 JSON에는 넣지 않는다. 외부 예측값은
ML이 계산한 `modelPredictedUnitPrice`만 제공한다.

## 검증 기록

- 단위 테스트 15개 통과
- 검증 내용:
  - 단위가격 환산
  - 세부유형·상품명·브랜드 집계와 지점 실제가격 생성
  - 판매업소 브랜드 분류
  - 시간순 특징 생성과 분할
  - 백엔드 JSON 변환
  - 저장 모델을 사용한 예측
  - 예측 실행이 모델 파일을 변경하지 않는지 확인
  - 다중 시점에서 직전 예측값이 다음 입력값으로 사용되는지 확인
  - 지점 직접가격 모델의 실제가격 목표값과 재귀 입력 검증
- 전체 원본 3,062,819행으로 전처리와 네 모델 학습을 실행했다.
- `ForecastHorizon 3` 전체 파이프라인 실행에 성공했다.
  - 세부유형 57건, 상품명 117건, 브랜드 618건, 지점 27,249건 생성
  - 지점 JSON은 약 18MB이므로 실제 API에서는 필터링과 페이지네이션이 필요

## 아직 구현되지 않은 부분

- CSV를 MySQL 테이블에 적재하는 로직
- 실제 SQL 마이그레이션과 seed
- DB의 `price_observation`을 직접 조회하는 ML 입력 경로
- 예측값을 저장할 `price_forecast` 테이블
- 예측 결과 DB upsert
- 실제 HTTP 전송과 백엔드 API
- 프론트엔드 차트 연결
- 자동 데이터 수집, 자동 재학습 스케줄, 모델 모니터링과 배포

ERD와 `docker-compose.yml`이 있다는 이유만으로 DB 연동이 완료됐다고 설명하지
않는다.

## 다음 작업 우선순위

1. 최신 `origin/main`을 반영하고 충돌 여부를 확인한다.
2. 사용자 승인 후 현재 `feature/ml-pipeline`에 commit 및 push한다.
3. CSV를 ERD 구조로 적재하는 DB loader와 마이그레이션을 구현한다.
4. `price_forecast` 테이블 및 예측값 upsert를 구현한다.
5. 상품·브랜드 필터와 지점 페이지네이션을 갖춘 조회 API를 구현한다.
6. 브랜드 차트와 지점 가격표를 프론트에 연결한다.
7. 실제값이 추가되면 지점별·다중 시점 성능을 별도로 평가한다.

## 상태 갱신 규칙

다음 중 하나가 발생하면 이 문서를 같은 작업에서 갱신한다.

- 기능 구현 또는 제거
- 테스트 결과 변경
- 데이터 수와 모델 성능 변경
- DB·백엔드·프론트 연동 상태 변경
- 브랜치, commit, push, PR 또는 merge 상태 변경
- 중요한 제한사항이나 다음 우선순위 변경

갱신할 때 완료된 구현, 로컬 미커밋 구현, 계획만 있는 작업을 명확히 구분한다.
